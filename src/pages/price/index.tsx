import {
  addPeriodItem,
  setStartDateForP,
  setEndDateForP,
  setPrice,
} from 'features/Loader/Period/model/period-reducer'
import { setActiveServiceId } from 'features/Services'
import { setActiveDivisions } from 'features/Filters'
import { setActiveArticleUnitItem } from 'features/Services'
import {
  setRedClass,
  clearSetRedClass,
} from 'features/Services/model/service-reducer'

import {
  updatePrice,
  setAllPeriodId,
  setResultWindow,
  setResultWindowHeader,
  setResultWindowStr,
  setResultWindowError,
} from 'features/Loader/Period/model/period-reducer'

export {
  addPeriodItem,
  setStartDateForP,
  setEndDateForP,
  setPrice,
  updatePrice,
  setActiveServiceId,
  setActiveDivisions,
  setActiveArticleUnitItem,
  setRedClass,
  clearSetRedClass,
  setAllPeriodId,
  setResultWindow,
  setResultWindowHeader,
  setResultWindowStr,
  setResultWindowError,
}
